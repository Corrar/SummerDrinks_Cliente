# Endpoint do sistema de atendimento (borda pública /public/:tenant/*).
# Em dev, o default é http://localhost:3000. Em prod, apontar para o domínio real.
VITE_API_URL=http://localhost:3000

# Slug do tenant. O app SÓ conhece o slug; o tenant_id interno nunca vaza.
VITE_TENANT=summer
